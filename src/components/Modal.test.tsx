import { describe, it, expect } from 'vitest';

describe.skip('Modal', () => {
  it('is visible when the isOpen prop is true', () => { });

  it('is not visible whe the isOpen prop is false', () => { });

  it('renders a close button when the hasCloseBtn prop is true', () => { });

  it('does not render a close button when the hasCloseBtn prop is false', () => { });

  it('closes when the close button is clicked', () => { });

  it('closes when the escape key is pressed', () => { });
});
