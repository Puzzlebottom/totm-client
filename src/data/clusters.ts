import { Cluster } from '../interfaces/Cluster';

const clusters: Cluster[] = [
  {
    id: 0,
    encounterId: 0,
    name: 'In a location',
    type: 'location',
    agentIds: [],
  },
];

export default clusters;
